---
"@huanglangjian/specs": minor
---

Add `generateManifests()` — language-agnostic Markdown documentation generator producing `server.md`, `client.md`, `subscriber.md` with JSON Schema references for AI-driven development
